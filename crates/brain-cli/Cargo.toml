[package]
name = "brain"
version.workspace = true
edition.workspace = true
description = "Agent-native semantic substrate: one graph for code, twin, decisions, tests, and docs"

[[bin]]
name = "brain"
path = "src/main.rs"

[dependencies]
brain-core = { workspace = true }
brain-store = { workspace = true }
brain-runtime = { workspace = true }
brain-observe = { workspace = true }
brain-index = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[dev-dependencies]
tempfile = { workspace = true }
